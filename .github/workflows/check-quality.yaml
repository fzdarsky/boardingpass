name: Code Quality

on:
  push:
    branches: [main, "**"]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache: true

      - name: Install golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.7.1
          install-only: true

      - name: Lint service
        run: make lint-service

      - name: Check generated code is up-to-date
        run: |
          make generate-service
          if [ -n "$(git status --porcelain)" ]; then
            echo "Error: Generated code is out of date. Please run 'make generate-service' and commit the changes."
            git status
            git diff
            exit 1
          fi
